# Copia este archivo a .env y ajusta los valores.

# mock  -> datos de demostración en el navegador (localStorage). No necesita API. (por defecto)
# live  -> la app consume tu API REST real.
VITE_API_MODE=mock

# Solo se usa cuando VITE_API_MODE=live
VITE_API_BASE_URL=http://localhost:8000/api
